---
type: session_log
status: archive
priority: p3
updated: 2026-05-25
context_policy: historical
owner: project
---

# Session Log: 2026-05-25

## Summary

- Bug-fix session on `dashboard/app.py` — three user-reported issues that all traced to one root cause: tables had no working GitHub links.

## Bugfix: Dashboard GitHub Links Broken

**Symptoms reported (PC / Streamlit Cloud):**
1. "Display bug" in the explorer table — `repo` cell rendered as a broken/non-navigable link.
2. Sidebar filter widgets felt unresponsive (no visible link change in the result table).
3. Top 15 Vibe table and Famous-Nothing table had no clickable GitHub links at all.

**Root cause:**
- Explorer table used `st.column_config.LinkColumn` on the `full_name` column, but `full_name` is `"owner/repo"` — not a URL. Streamlit treated it as a relative href, producing 404s on PC.
- Top 15 Vibe and Famous-Nothing tables never had a link column; `vibe_scores.csv` only carries `full_name` (no `html_url`).

**Fix:**
- Explorer: switch `LinkColumn` to the real `html_url` column, keep `full_name` as a plain text "repo" column for readability.
- Top 15 Vibe + Famous-Nothing: merge `vibe` rows against `repos.set_index("full_name")["html_url"]` to attach the real URL, then add a `LinkColumn("GitHub", display_text="🔗 打開")` as the first column.

**Validation:**
- `python -m pytest -q` → 22/22 passed.
- `python -c "import ast; ast.parse(open(...))"` → syntax OK.
- `ruff` not installed in this venv (only `.idea/ruff.xml` config present) — skipped.

**Files changed:**
- `dashboard/app.py` (3 dataframe blocks: Top 15 Vibe, Famous-Nothing, Explorer).

---

## COMPLETED: TASK.012 - REVIEW-FIXES: 修正清單 P0/P1/P2 全集

來源：使用者提供的 `GitHub趨勢分析專案_修正清單.docx`（外部審查建議）。8 項全部完成。

### P0 — 用詞中性化 + 限制章節

1. **用詞中性化（涵蓋 src + dashboard + 報告 + 簡報）**
   - Tier rename: `garbage` → `低資訊密度`、`suspicious` → `待檢視`、`legitimate` → `訊號完整`。
   - Score rename: vibe-coding garbage → 公開 metadata 完整度風險評分 (Metadata Completeness Risk Score)。
   - Famous-Nothing → 高關注低描述。
   - farming「集中區」、「甜蜜點」改為「觀察性結論」「中段 stars 區段集中」並明確標註「非因果推論」。
   - 在每處新框架附上：「指標衡量公開產出，不衡量作者本人，也不是對 vibe-coding 這個編程方式本身的評斷。」免責聲明。
   - 改的檔案：`src/analyze_vibe.py`、`dashboard/app.py`、`README.md`、`outputs/vibe_coding_analysis.md`（已重新生成）、`outputs/report.md`、`docs/presentation.md`、`src/prepare_vibe_validation.py`。
   - CSV `tier` 欄值已更新為中文（重新跑 `analyze_vibe.py` 後）。Dashboard 自帶 threshold slider 邏輯會即時 recompute tier，所以舊 CSV 也能向後相容顯示。

2. **限制與威脅章節 (Limitations & Threats to Validity)**
   - README 與 `outputs/report.md §10` 重組為三類結構：A. 資料限制 / B. 方法限制 / C. 外推限制。
   - 新增 E4：Search API 已 sort=stars desc 帶入「熱門優先」偏差（之前沒講清楚）。

### P1 — 註解、版本、驗證

3. **`src/collect_repos.py` 註解修正** — `MAX_PAGES 5 / 500 repos` → `10 by default → 1000 repos, the GitHub Search API hard limit`。

4. **`pyproject.toml` 放寬 Python 版本** — `requires-python = ">=3.14"` → `>=3.11`。Grep 過 `src/` 沒有 3.13/3.14 專屬語法（`type X = ...` PEP 695、TypeAlias、新增的 stdlib 等都沒用到）。README badge + 「前置需求」也同步更新成 `3.11+`。

5. **分類抽樣驗證 scaffolding**
   - 新增 `src/prepare_classification_validation.py`：對 9 個 category 做 stratified random sampling，每類別 ~5 筆 = 50 筆；輸出 utf-8-sig CSV 給人工標註 `agree` / `correct_category` / `notes`。
   - 隨機種子固定為 42，便於重現。
   - README「跑單元測試」下方新增「產生人工驗證樣本」段，列出兩條指令（classification + vibe）。

### P2 — 補強分析、字典、精簡

6. **Other 類別深入分析**
   - 新增 `src/analyze_other.py`，產出 `outputs/other_breakdown.md`。
   - 內容：Other 內 top 10 主要語言、top 20 description 關鍵字、top 10 topics、6 個候選新類別（Education / Design / Blockchain / Finance / Hardware / Documentation）的命中率。
   - 觀察重點：Other 內 `trading`(204) + `polymarket`(189) 訊號極強 → 「Finance / Trading」候選類別估計 ~211 個 repo，下一輪規則式分類器若加 Finance 大類，Other 桶可能顯著縮水。

7. **README Data Dictionary 章節**
   - 在「架構」與「檔案結構」之間新增 §Data Dictionary，按用途分組（Identifier / Descriptive / Time / Popularity / Derived / Classification / Metadata 完整度評分 / 長表）。
   - 衍生欄位列出公式（`age_days = max((run_date - created_at).days, 1)` 等）。
   - 明確標註 `watchers` = `stars` 為 API 限制，**分析應避免使用**。

8. **README 精簡**
   - 把 50 行的 file-structure tree 砍到 13 行，並指向 `docs/architecture.md` 看詳情。
   - 8-訊號 scoring 表收斂為 7 列「訊號類別」summary，full table 留在 `outputs/vibe_coding_analysis.md`。
   - 結果：README 從 478 行 / 15.6 KB → 451 行 / 14.6 KB。

### Validation

- `python -m pytest -q` → 22/22 passed。
- 所有受影響 Python 檔案 `ast.parse()` 通過。
- `python -m src.analyze_vibe`、`python -m src.analyze_other`、`python -m src.prepare_classification_validation`、`python -m src.prepare_vibe_validation` 都成功跑完，輸出檔案皆已產生。
- Vibe sample CSV 從 58 筆 → 重新生成後仍為 58 筆（18 低資訊密度 + 20 待檢視 + 20 訊號完整）。
- Classification sample CSV 為 50 筆，跨 9 類別 stratified random。

### 後續手動工作（不在這次 scope）

- 人工標註 50 筆分類樣本與 58 筆 metadata 樣本，回填一致率 / precision / recall 到 §限制與威脅與 §10。
- 若要在分類器加 Finance/Trading 類別：修 `src/classify.py` keyword 表 + 加 unit test + 更新 ADR-0004。

---

## COMPLETED: TASK.013 - INCREMENTAL-REFRESH: stale-aware daily refresh + prune

Plan: `C:\Users\shawn\.claude\plans\refresh-delegated-crab.md`. 來源：使用者觀察 daily-refresh「每次都重抓」想要只更新有變動的 repo。

### 變更

1. **新 `src/cache_manifest.py`** — schema `{_manifest_version, repos: {id: {languages_fetched_at, topics_fetched_at, last_seen_at}}}`。
   - `is_stale()` 三層 fallback：cache 缺失 / 不可解析 → stale；無 manifest 條目 → 比對 `cache_path.stat().st_mtime`；有條目 → 比對 manifest fetched_at。
   - 所有 timestamp 皆 tz-aware UTC。`save_atomic()` 用 `os.replace`（跨平台 atomic）。
   - `bootstrap_existing()` 偵測 `repos` 為空時把既存 cache 全 stamp 為 fresh，避免第一次 upgrade 觸發 ~2000 call surge。
   - `prune()` 內建 `max_pruned=200` ceiling。

2. **`src/collect_all.py`** wire-in
   - `discover_repos_on_disk` 改為「最新檔案 wins」並抽 `pushed_at`。
   - `collect_per_repo` 接 manifest、用 `is_stale` 判斷、try/finally 每 50 筆 save 一次。
   - `main()` 新增 bootstrap_existing、mark_seen all、prune；進度檔加 `manifest.bootstrapped_stamps`、`pruned_repo_ids`、`pruned_count`、每階段 `stale_refetched`。

3. **`.github/workflows/daily-refresh.yml`** 把 `data/raw/_cache_manifest.json` 加進 cache `path:`。

4. **`tests/test_cache_manifest.py`** 16 筆純 unit test (tmp_path, 無 HTTP)：load/save_atomic、is_stale 真值表、mark_*、bootstrap sentinel、prune ceiling。

### 驗證

- `python -m pytest -q` → **38/38** 通過（原 22 + 新 16）。
- Offline smoke：`discover_repos_on_disk()` 回傳 999 repos 含 `pushed_at`；`bootstrap_existing` stamp 1998 條（999×2）；之後 `is_stale()` 對所有 repo 都回 False → **第一次 upgrade 不會 surge**。
- YAML 解析通過、cache 三個 path 都在。

### 預期效能

- 暖快取下每天 ~50-200 stale-refetch + 新進 window ~50-100 repo，總 ~300 calls × 0.8s = ~4 min API time，遠在 90-min workflow timeout + 5000/hr rate limit 內。
- 60 天 buffer prune 自動清掉久未出現的 repo cache。

---

## TASK.008 partial progress — README 數據同步 + 標記 3/4 完成

實際檢視 TASK.008 acceptance criteria 後發現大部分早已完成，只是 active.md 未同步：

- README 已 embed `<img src="outputs/figures/dashboard_hero.png">` (L5-11)，圖檔本身仍 blocked on user。
- README L26-35 TL;DR section 已存在。
- `docs/presentation.md` 138 行完整 5 分鐘腳本已存在。

順手對齊三處過時數字：
- README L34 「Python 3.14 + ... 全程 22 個 pytest」→「Python 3.11+ + ... 全程 38 個 pytest」。
- README L276 「**22 tests passed**」→「**38 tests passed**」。
- README L402 「← 22 個 pytest」→「← 38 個 pytest」。

驗證來源：`python -m pytest --collect-only -q` 收集到 38 tests；`pyproject.toml` `requires-python = ">=3.11"`。

`active.md` 中 TASK.008 status 改為 `in_progress (3/4 done; hero shot blocked on user-supplied screenshot)`，三個已完成 criteria 打勾，hero shot 維持 `[ ]`。順帶修正 frontmatter `updated:` 後誤插的空行。

---

## COMPLETED: TASK.014 - CLASSIFIER-V2: Finance/Trading + keyword gap fill (ADR-0004 revision)

Plan: `C:\Users\shawn\.claude\plans\splendid-questing-lighthouse.md`. 來源：用戶在 plan mode 對「擴抓取 vs 擴單字庫」兩個方向比較後選 Stage 1（擴單字庫）。

### 變更

1. **`src/classify.py`** — `CATEGORY_RULES`：
   - 新增第 3 類 **Finance/Trading**（priority `AI/ML > Security > Finance/Trading > DevOps > Data > Web > Mobile > Game > CLI/Tooling > Other`），keyword 含 `trading`/`polymarket`/`arbitrage`/`pumpfun`/`hyperliquid`/`trading-bot` 家族 + `defi`/`dex`/`mev`/`hft` + Web3 訊號 `crypto`/`solana`/`wallet`/`fintech`/`finance`。Web3 暫時併入 Finance，避免一次切兩個新類別；如驗證顯示獨立 cluster 則由後續 ADR-0005 拆分。
   - AI/ML 補：`claude-code`、`mcp`、`codex`、`cursor`、`langchain`、`langgraph`、`vector-database`、`vector-search`（這些都是 README 自己 top topics 但 keyword 表漏的）。
   - Web 補：`shadcn`、`tailwindcss`、`astro`、`nuxt`、`remix`、`solidjs`、`vite`。
   - Mobile 補：`swiftui`、`jetpack-compose`、`expo`、`compose-multiplatform`。
   - Security 補：`oauth`、`oauth2`、`sso`、`siem`、`waf`、`zero-trust`。

2. **`tests/test_classify.py`** — 加 3 條 unit test：
   - `test_classify_finance_trading_polymarket` — polymarket arbitrage bot → Finance/Trading。
   - `test_classify_ai_ml_claude_code_topic` — 只靠 `claude-code` topic → AI/ML（驗證新 keyword）。
   - `test_classify_priority_ai_beats_finance` — `llm` + `trading` 同時命中 → AI/ML（priority order 正確）。

3. **`docs/adr/0004-rule-based-classification.md`** — 維持 `proposed`，加 Revision Log section 完整記錄 trigger / changes / before-after table / trade-offs / new finding / validation status。

4. **`README.md`** — TL;DR + 三個關鍵發現 + §完整圖表結果 §5 + §限制與威脅 B.4/C 重新編號。新增 §限制 B.7 記錄 Finance/Trading fork-farming 疑似訊號 + 三種可能解釋 + future work。

5. **`outputs/report.md`** — 摘要、§7.5 表格、§9.1/9.2 + 新增 §9.3「Finance/Trading 類的反向 fork 異常」、§9.5（原 9.4）數字、§10.1/10.2 修訂註記、§11 結論加第 6 點。

6. **`docs/presentation.md`** — 開場 999 → 1000、研究方法 9 大類 → 10 大類、發現 1 與發現 2 全改寫、結論一句話加「加密交易 fork-farming 暗流」。

### 驗證

- `python -m pytest -q` → **41/41 passed**（38 + 3 new）。
- `python -m src.build_charts` 跑通；`category.value_counts()` 出現新 `Finance/Trading: 45` 行。
- `python -m src.analyze_vibe`、`python -m src.analyze_other`、`python -m src.prepare_classification_validation` 全部跑通。
- 重生 figures：`outputs/figures/category_distribution.png`、`category_avg_stars.png`、`top_languages.png`、`top_topics.png`、`stars_distribution.png`、`forks_vs_stars.png`、`daily_new_repos.png`。
- 重生 reports：`outputs/summary_stats.md`、`vibe_coding_analysis.md`、`other_breakdown.md`、`data/processed/classification_validation_sample.csv`（50 筆，跨 10 類 stratified）。

### Spot checks（執行後驗證 plan 提出的 Open Question）

- **Polymarket repos**：5 筆中 4 筆 → Finance/Trading；1 筆 (`polymarket-developers/mkzmbd4`) → Other 因為 polymarket 只在 owner 字串，分類器不看 owner（acceptable, 在現行 design scope 之外）；1 筆 (`trading-2028/polymarket-ai-trading`) → AI/ML 是正確結果（`ai` keyword + 描述含 LLM → priority AI 勝）。
- **`claude-code`/`mcp`/`cursor`/`codex` topic 82 筆**：100% 落 AI/ML，無逃出。
- **Priority order 驗證**：未發現 AI-driven trading bot 被 Finance/Trading 偷走的 case。

### 數據前後對比

| Category | Before | After | Δ |
|---|---:|---:|---:|
| AI/ML | 365 (36.5%) | **389 (38.9%)** | +24 |
| Other | 420 (42.0%) | **375 (37.5%)** | **−45** |
| Web | 74 (7.4%) | 50 (5.0%) | −24 |
| Finance/Trading | — | **45 (4.5%)** | new |
| Mobile | 41 (4.1%) | 42 (4.2%) | +1 |
| CLI/Tooling | 30 | 30 | 0 |
| Game | 26 | 25 | −1 |
| Security | 21 | 22 | +1 |
| Data | 11 | 11 | 0 |
| DevOps | 12 | 11 | −1 |

Other 桶 −4.5pp（不是 plan 預估的 −20pp，因為 `outputs/other_breakdown.md` 的 211 估計是 keyword 重複命中）；Web 因 trading-themed React/dashboards 移走而縮，符合 ADR-0004 既有 ambiguous-repo trade-off。

### 計畫外發現（值得獨立報告）

1. **Finance/Trading 類 fork:star = 767% (median 9.9×)**：top 5 polymarket bot 全部 30× forks vs stars (`POLYMARKET-TRADER-LAB/Polymarket-trading-bot` 289 stars / 4262 forks 即 14.7×；`mclaeo/polymarket-copytrade` 144/3918 即 27.2×)。**疑似自動化 fork-farming**，但本研究無法 100% 區分 (a) fork-farming / (b) 「下載即 fork」部署 pattern / (c) GitHub 演算法異常。已寫入 report.md §9.3 + README §限制 B.7 + presentation 發現 2。

2. **Finance/Trading 類 metadata 完整度是所有類別中最好的之一**（mean score 0.78 vs 整體 1.35，全部 45 個都在「訊號完整」tier）。原 plan 預測「trading bot 是 metadata 最低 cluster」**錯了**。但與「fork-farming 疑似」共構出特殊型態：「metadata 漂亮 + fork 暴衝 + stars 平庸」— 可能反映「精心打理門面以吸引部署」的策略。

### 已知 limitation

- Priority order 與 keyword 設計仍有主觀性，待 TASK.010 人工標註 50 筆 stratified validation 樣本後可量化 agreement rate。
- 1 筆 (`polymarket-developers/mkzmbd4`) 因 polymarket 只在 owner 字串而落 Other — 若要納入需擴張 `_tokens()` 看 `full_name`，但會引入「awesome-llm」這類 owner 雜訊，目前不做。

---

## COMPLETED: TASK.015 - DASHBOARD-DYNAMIC-NARRATIVE: 消除 daily-refresh 後 stale 文字

來源：用戶在 Streamlit dashboard 跑通後問「幫我增加動態編碼，避免 streamlit 每天更改後文字內容卻無變化」。

### 7 處硬編 narrative 改為 data-driven

| 位置 | 改前 | 改後 |
|---|---|---|
| §4 takeaway 結尾 | 硬編「AI/ML 類別 stars 高 forks 低」(假設 AI/ML 永遠是 fork:star 最低) | 用 `forkstar_extremes()` 動態算 lowest + highest fork:star 類別；若 highest ≥ 200% 自動加「異常反向疑似 fork-farming」警語 |
| §5 `_cat_shape` 三段 if/elif/else | 硬編「AI/ML+Other」前兩名、「7/8 個類別」 | 用 `top_n_categories()` 取真正前兩名；「其餘 N 個類別」改用 `nunique() - 2` |
| §5 explainer plain | 硬編「分到 10 大類別」 | 改 `{n_cats} 大類別` 動態 |
| §5b sankey explainer | 硬編「右邊是 10 大分類」 | 改 `{n_cats} 大分類` 動態 |
| §7 stacked explainer | 硬編「AI/ML 與 Other 最厚」「Web/Mobile 小類別」 | 用 `top_n_categories()` + `smallest_n_categories()` 取得真正前兩名/末兩名 |
| Explorer caption | 「篩選 999 個 repo」 | 改 `f"依條件篩選 {len(repos)} 個 repo"` 動態 |
| Footer caption | 「54% repo 無 topics」 | 動態算 `(1 - topics["repo_id"].nunique() / len(repos)) * 100` |

### 新加 3 個 narrative helper

於 `dashboard/app.py` narrative helpers 區塊（接續既有 `label_correlation` 等）：

```python
top_n_categories(repos_df, n=2)          # [(cat, pct), ...] 前 n 名
smallest_n_categories(repos_df, n=2)     # [(cat, count), ...] 末 n 名
forkstar_extremes(repos_df, min_repos=10) # ((lo_cat, lo_pct), (hi_cat, hi_pct))
```

設計理念：helpers 只回傳結構化值（不直接拼字串），具體 wording 留在使用點，這樣每個 section 可以有自己的口吻。

### 驗證

- `python -c` 載 repos.csv 模擬 helper 行為：
  - `top_n_categories` → `[('AI/ML', 38.9), ('Other', 37.5)]`
  - `smallest_n_categories` → `[('Data', 11), ('DevOps', 11)]`
  - `forkstar_extremes` → `lowest=Mobile 6%, highest=Finance/Trading 769%`
- AST parse `dashboard/app.py` 通過。
- Streamlit headless smoke run on `:8502`：startup log clean、HTTP 200、`/_stcore/health = ok`。
- `python -m pytest -q` → **41/41 passed**（沒新增 test，dashboard helpers 目前在 streamlit script 內 import 不易；如要 unit test 需把 helpers 抽出到 `dashboard/narrative.py` module，留作後續 task）。
- **副作用獎金**：原硬編「AI/ML fork:star 17% 全類別最低」其實是錯的 — Mobile 才是 6%（最低）。動態化把這個錯誤也順手修了。Finance/Trading 769% 會自動觸發「異常反向疑似 fork-farming」warning，與 README/report.md §9.3 一致。

### 未涵蓋（保留硬編但 ok）

- §5 explainer 中的 priority order「AI/ML > Security > Finance/Trading > ...」：此為 code 邏輯不是 data，若日後 ADR 再改 priority 需手動同步 dashboard 文案；可在後續 task 從 `src.classify.CATEGORY_RULES` 動態 import 自動同步。
- §🔬 metadata 完整度區塊內 narrative 已是動態（用 slider 重算 tier 並 dynamic 拼字串），無變更。
- 元資料表頭、tier 名稱（`TIER_LOW` 等常數）— 本意就是固定中文標籤，非 daily-stale 對象。